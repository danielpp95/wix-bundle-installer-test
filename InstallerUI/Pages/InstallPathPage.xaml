<local:BasePage x:Class="InstallerUI.Pages.InstallPathPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:InstallerUI.Pages">
    <StackPanel>
        <TextBlock Text="Installation folder" FontSize="14" Foreground="#595959" Margin="0,0,0,10" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="PathTextBox" Grid.Column="0"
                     Height="26"
                     VerticalContentAlignment="Center"
                     BorderBrush="#8A8A8A"
                     Padding="4,0,0,0" />

            <Button x:Name="BrowseButton" Grid.Column="1"
                    Content="Browse"
                    Width="90" Height="26"
                    Margin="8,0,0,0"
                    Background="#EEEEEE"
                    BorderBrush="#8A8A8A"
                    Click="OnBrowseClick" />
        </Grid>
    </StackPanel>
</local:BasePage>
