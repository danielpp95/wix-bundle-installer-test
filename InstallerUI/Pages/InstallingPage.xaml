<UserControl x:Class="InstallerUI.Pages.InstallingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ItemsControl x:Name="PackagesList" Grid.Row="0" Margin="0,0,0,10">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="120" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding DisplayName}" Foreground="#595959" />
                        <TextBlock Grid.Column="1" Text="{Binding StatusText}" Foreground="#595959" HorizontalAlignment="Right" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ProgressBar x:Name="OverallProgressBar" Grid.Row="1" Height="18" Margin="0,4,0,10"
                     Minimum="0" Maximum="100" Foreground="#8FC97D" />

        <TextBox x:Name="LogTextBox" Grid.Row="2"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="11"
                 Background="#FAFAFA"
                 BorderBrush="#8A8A8A"
                 TextWrapping="NoWrap" />
    </Grid>
</UserControl>
