<local:BasePage x:Class="InstallerUI.Pages.InstallingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:InstallerUI.Pages"
             xmlns:models="clr-namespace:InstallerUI.Models"
             xmlns:controls="clr-namespace:InstallerUI.Controls">
    <Grid MinWidth="200">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ItemsControl x:Name="PackagesList" Grid.Row="0" Margin="0,0,0,14">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="120" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding DisplayName}" Style="{StaticResource Text.Body}" />
                        <TextBlock Grid.Column="1" Text="{Binding StatusText}" HorizontalAlignment="Right">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource Text.Body}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="{x:Static models:PackageStepStatus.Done}">
                                            <Setter Property="Foreground" Value="{DynamicResource Brush.Status.Success}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="{x:Static models:PackageStepStatus.Failed}">
                                            <Setter Property="Foreground" Value="{DynamicResource Brush.Status.Error}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="{x:Static models:PackageStepStatus.RollingBack}">
                                            <Setter Property="Foreground" Value="{DynamicResource Brush.Status.Warning}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="{x:Static models:PackageStepStatus.Waiting}">
                                            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Secondary}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <controls:LoaderControl x:Name="Loader" Grid.Row="1" Margin="0,0,0,14" />

        <TextBox x:Name="LogTextBox" Grid.Row="2"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="11"
                 Background="{DynamicResource Brush.Surface.Muted}"
                 BorderBrush="{DynamicResource Brush.Border}"
                 TextWrapping="NoWrap" />
    </Grid>
</local:BasePage>
