<local:BasePage x:Class="InstallerUI.Pages.ErrorPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:InstallerUI.Pages">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageText" Grid.Row="0"
                   Style="{StaticResource Text.Body}"
                   Foreground="{DynamicResource Brush.Status.Error}"
                   Text="Setup failed." />

        <TextBlock Grid.Row="1" Text="Details:" Style="{StaticResource Text.Caption}" Margin="0,14,0,4" />

        <TextBox x:Name="DetailsTextBox" Grid.Row="2"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="11"
                 Background="{DynamicResource Brush.Surface.Muted}"
                 BorderBrush="{DynamicResource Brush.Border}"
                 TextWrapping="NoWrap" />
    </Grid>
</local:BasePage>
