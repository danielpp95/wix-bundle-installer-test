<UserControl x:Class="InstallerUI.Pages.ErrorPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageText" Grid.Row="0"
                   TextWrapping="Wrap"
                   FontSize="14"
                   Foreground="#C0392B"
                   Text="Setup failed." />

        <TextBlock Grid.Row="1" Text="Details:" Foreground="#595959" Margin="0,14,0,4" />

        <TextBox x:Name="DetailsTextBox" Grid.Row="2"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="11"
                 Background="#FAFAFA"
                 BorderBrush="#8A8A8A"
                 TextWrapping="NoWrap" />
    </Grid>
</UserControl>
