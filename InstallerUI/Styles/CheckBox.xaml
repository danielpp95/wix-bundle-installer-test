<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Checkmark stroke lifted from Assets/icons/checkmark-frame.svg (the circular frame
         from that icon is dropped - this box already draws its own frame). SVG and WPF
         path-data grammars are compatible, so the "d" attribute is usable verbatim; the
         icon's 192x192 viewBox is mapped onto the 14x14 box via Viewbox/Stretch. -->
    <Geometry x:Key="Icon.Checkmark">M129.17139,69.17139l5.65723,5.65723L84,125.65674L57.17139,98.82861l5.65723-5.65723L84,114.34326L129.17139,69.17139z</Geometry>

    <Style x:Key="CheckBox.Default" TargetType="CheckBox">
        <Setter Property="FontFamily" Value="{StaticResource Font.Regular}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="Box"
                                Width="18" Height="18"
                                Background="{DynamicResource Brush.Surface}"
                                BorderBrush="{DynamicResource Brush.Text.Secondary}"
                                BorderThickness="1.5"
                                CornerRadius="3"
                                VerticalAlignment="Center">
                            <Viewbox Width="10" Height="10" Stretch="Uniform">
                                <Canvas Width="192" Height="192">
                                    <Path x:Name="Check"
                                          Data="{StaticResource Icon.Checkmark}"
                                          Fill="{DynamicResource Brush.Text.OnPrimary}"
                                          Visibility="Collapsed" />
                                </Canvas>
                            </Viewbox>
                        </Border>
                        <ContentPresenter Margin="8,0,0,0"
                                           VerticalAlignment="Center"
                                           RecognizesAccessKey="True" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="{DynamicResource Brush.Action.Primary}" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Brush.Action.Primary}" />
                            <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Brush.Action.Primary}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Brush.Border.Disabled}" />
                            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Disabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
